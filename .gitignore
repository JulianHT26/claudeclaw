node_modules/
dist/
agent/runner/dist/
store/
data/
groups/
logs/
.env

# Installable extensions (per-instance, not checked in)
extensions/

# Brainstorm sessions
.superpowers/

# Permisos locales de Claude Code (auto-aprobaciones acumuladas por sesión,
# no una decisión de equipo) — settings.json compartido sí se versiona.
.claude/settings.local.json
